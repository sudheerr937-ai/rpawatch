{
  "name": "rpawatch-webhook",
  "version": "1.0.0",
  "description": "RPAWatch webhook receiver — receives AppSignal alerts and triggers UiPath Orchestrator remediation",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
